"""Inbound events: external sources -> per-source forum topics.

Zero LLM processing on receipt — events are rendered and posted as inert
messages.  The user acts on an event by replying in its topic.
"""
